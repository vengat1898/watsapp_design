import 'package:flutter/material.dart';

class watsappcamera extends StatelessWidget {
  const watsappcamera({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
