import 'package:flutter/material.dart';

class watsappcalls extends StatelessWidget {
  const watsappcalls({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('calls'),
    );
  }
}
