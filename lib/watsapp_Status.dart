import 'package:flutter/material.dart';

class watsappstatus extends StatelessWidget {
  const watsappstatus({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
